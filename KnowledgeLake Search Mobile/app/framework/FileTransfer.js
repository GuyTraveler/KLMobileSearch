define(["system", "jquery"], function (system, $) {
    var fileTransfer = function () {
        var self = this;
        
        self.transfer = function (uri) {
            // download to downloads folder
        }
    }
    
    return fileTransfer;
});